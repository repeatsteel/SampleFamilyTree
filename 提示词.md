
请修复一下问题
1. 字体切换成 fonts文件中 fontawesome-webfont 字体，可用的库位于 lib文件夹中
2. 成员管理（members） 和 分支（branches）从左右切换成上下排布


请修复一下问题
1. 成员管理（members）中列增加宽度，避免出现了字符截断
2. 每条数据当前界面仅静态展示，不可修改
3. 右侧的保存按钮变更为修改按钮，增加单独选项卡修改信息 
    -  出生	去世 修改为时间选择框 
    -  字辈(Zibei)下拉列表选择框
4. 父ID	母ID	配偶ID(双向)	子女IDs（逗号分隔） 展示信息 仅仅显示名称，方便可读性





导出成员JSON 按钮点击后  换行符号直接显示出来 导致不方便阅读


成员管理（members） 和编辑的弹框中均缺少 bio 字段  请添加
图层逻辑不合理



